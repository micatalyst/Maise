export const metadata = {
  title: 'Tornar Acessível',
  description: 'Veja aqui as quatro vertentes pelas quais poderá tornar o seu conteúdo mais acessível.',
};

export default function ProdutoLayout({ children }) {
  return <>{children}</>;
}
